什么是跨域请求限制？
浏览器向服务器发送请求后，收到来自服务器的响应，对响应进行校验，如果响应中允许的域和浏览器页面的域同源，那么浏览器会接收这个响应，否则会报跨域错误

为什么要限制跨域请求？

如何解决跨域限制？
在浏览器和目标服务器之间使用中转服务器转发请求和响应

为什么利用postman或者非浏览器（中转服务器）方式不做跨域限制？
--因为浏览器端存有用户信息，而服务器或者postman不存有cookie等信息？